diff --git a/Sources/CodeEditSourceEditor/Find/FindViewController.swift b/Sources/CodeEditSourceEditor/Find/FindViewController.swift
index a9e2dd3..0ffb8dc 100644
--- a/Sources/CodeEditSourceEditor/Find/FindViewController.swift
+++ b/Sources/CodeEditSourceEditor/Find/FindViewController.swift
@@ -6,6 +6,7 @@
 //
 
 import AppKit
+import Combine
 import CodeEditTextView
 
 /// Creates a container controller for displaying and hiding a find panel with a content view.
@@ -24,7 +25,9 @@ final class FindViewController: NSViewController {
 
     var childView: NSView
     var findPanel: FindPanelHostingView
+    var findPanelHeightConstraint: NSLayoutConstraint!
     var findPanelVerticalConstraint: NSLayoutConstraint!
+    var cancellables: Set<AnyCancellable> = []
 
     /// The 'real' top padding amount.
     /// Is equal to ``topPadding`` if set, or the view's top safe area inset if not.
@@ -64,10 +67,12 @@ final class FindViewController: NSViewController {
         findPanel.layer?.zPosition = 1000
 
         findPanelVerticalConstraint = findPanel.topAnchor.constraint(equalTo: view.topAnchor)
+        findPanelHeightConstraint = findPanel.heightAnchor.constraint(equalToConstant: viewModel.panelHeight)
 
         NSLayoutConstraint.activate([
             // Constrain find panel
             findPanelVerticalConstraint,
+            findPanelHeightConstraint,
             findPanel.leadingAnchor.constraint(equalTo: view.leadingAnchor),
             findPanel.trailingAnchor.constraint(equalTo: view.trailingAnchor),
 
@@ -77,6 +82,19 @@ final class FindViewController: NSViewController {
             childView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
             childView.trailingAnchor.constraint(equalTo: view.trailingAnchor)
         ])
+
+        viewModel.$mode
+            .sink { [weak self] _ in
+                guard let self else { return }
+                self.findPanelHeightConstraint.constant = self.viewModel.panelHeight
+                self.findPanel.invalidateIntrinsicContentSize()
+                if self.viewModel.isShowingFindPanel {
+                    self.setFindPanelConstraintShow()
+                } else {
+                    self.setFindPanelConstraintHide()
+                }
+            }
+            .store(in: &cancellables)
     }
 
     override func viewWillAppear() {
diff --git a/Sources/CodeEditSourceEditor/Find/PanelView/FindPanelHostingView.swift b/Sources/CodeEditSourceEditor/Find/PanelView/FindPanelHostingView.swift
index dedb9bd..ee40f54 100644
--- a/Sources/CodeEditSourceEditor/Find/PanelView/FindPanelHostingView.swift
+++ b/Sources/CodeEditSourceEditor/Find/PanelView/FindPanelHostingView.swift
@@ -9,7 +9,7 @@ import SwiftUI
 import AppKit
 import Combine
 
-/// A subclass of `NSHostingView` that hosts the SwiftUI `FindPanelView` in an 
+/// A lightweight container that hosts the SwiftUI `FindPanelView` in an
 /// AppKit context.
 ///
 /// The `FindPanelHostingView` class is responsible for:
@@ -21,31 +21,52 @@ import Combine
 ///
 /// This class is essential for integrating the SwiftUI-based find panel into the AppKit-based
 /// text editor.
-final class FindPanelHostingView: NSHostingView<FindPanelView> {
+final class FindPanelHostingView: NSView {
     private weak var viewModel: FindPanelViewModel?
 
     private var eventMonitor: Any?
+    private let hostingController: NSHostingController<FindPanelView>
 
     init(viewModel: FindPanelViewModel) {
         self.viewModel = viewModel
-        super.init(rootView: FindPanelView(viewModel: viewModel))
+        self.hostingController = NSHostingController(rootView: FindPanelView(viewModel: viewModel))
+        if #available(macOS 13.0, *) {
+            // The find panel is height-constrained by AppKit. Letting NSHostingController
+            // export SwiftUI sizing constraints causes ViewThatFits to remeasure both
+            // find-panel layouts during every editor/window resize.
+            hostingController.sizingOptions = []
+        }
 
-        self.translatesAutoresizingMaskIntoConstraints = false
+        super.init(frame: .zero)
 
-        self.wantsLayer = true
-        self.layer?.backgroundColor = .clear
+        translatesAutoresizingMaskIntoConstraints = false
+        wantsLayer = true
+        layer?.backgroundColor = .clear
 
-        self.translatesAutoresizingMaskIntoConstraints = false
-    }
+        let hostedView = hostingController.view
+        hostedView.translatesAutoresizingMaskIntoConstraints = false
+        addSubview(hostedView)
 
-    @MainActor @preconcurrency required init(rootView: FindPanelView) {
-        super.init(rootView: rootView)
+        NSLayoutConstraint.activate([
+            hostedView.topAnchor.constraint(equalTo: topAnchor),
+            hostedView.bottomAnchor.constraint(equalTo: bottomAnchor),
+            hostedView.leadingAnchor.constraint(equalTo: leadingAnchor),
+            hostedView.trailingAnchor.constraint(equalTo: trailingAnchor)
+        ])
     }
 
     required init?(coder: NSCoder) {
         fatalError("init(coder:) has not been implemented")
     }
 
+    override var intrinsicContentSize: NSSize {
+        NSSize(width: NSView.noIntrinsicMetric, height: viewModel?.panelHeight ?? 28)
+    }
+
+    override var fittingSize: NSSize {
+        NSSize(width: 0, height: viewModel?.panelHeight ?? 28)
+    }
+
     deinit {
         removeEventMonitor()
     }
